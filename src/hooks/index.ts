/**
 * Hook Writer for Claude Code
 *
 * Export the hook writer functionality so users can import it in their
 * Claude Code hooks configuration.
 */

export { HookWriter, getHookWriter } from './HookWriter.js';
